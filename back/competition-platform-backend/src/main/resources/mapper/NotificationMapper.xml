<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.competition.mapper.NotificationMapper">

    <insert id="batchInsert" parameterType="list">
        INSERT INTO notification (user_id, title, content, type, related_id, is_read, create_time) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.userId}, #{item.title}, #{item.content}, #{item.type}, #{item.relatedId}, 0, NOW())
        </foreach>
    </insert>

</mapper>